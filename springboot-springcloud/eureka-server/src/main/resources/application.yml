server:
  port: 8761
eureka:
  instance:
    hostname: eurake-server # eureka 实例主机名
  client:
    register-with-eureka: false # 不注册自己
    service-url:
      defaultZone: http://localhost:8761/eureka/
